# Node/Next
node_modules/
.next/
.turbo/
.vercel/
.npmrc
pnpm-lock.yaml
yarn.lock
# package-lock.json is tracked on purpose: CI resolves dependencies from
# scratch, and an unpinned transitive upgrade broke the build with no code
# change. Keep the lockfile committed so builds are reproducible.
build/
dist/
coverage/
*.tsbuildinfo

# Env / secrets
.env*

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.tmp

# Logs
*.log
npm-debug.log*
.vercel


#Sample-files
sample-files/